#include<stdio.h>
int main() {
int arr[100], array[4][5];
int i,n,m,j;
printf("Enter the size of array");
   scanf("%d",&n);
   printf("Enter the elements of array");
   for(i=0;i<n;i++){
       scanf("%d",&arr[i]);
   }
   checksum(arr,n);
   printf("\n Enter the number of rows and columns of matrix");
   scanf("%d %d",&m,&n);
   printf("Enter the elements of matrix");
   for(i=0;i<m;i++){
       for(j=0;j<n;j++){
           scanf("%d",&array2[i][j]);
       }
   }
   sortalternatecolumns(array2,m,n);
   return 0;
}
